{% extends 'base.html.twig' %}

{% block title %}Hello MainController!
{% endblock %}

{% block body %}
	{{form_start(form)}}
	{{form_end(form)}}

	<pre>
        {{ dump(annonces) }}
    </pre>
{% endblock %}
